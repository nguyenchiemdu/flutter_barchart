name: bar_chart
description: A new Flutter project.
repository: https://github.com/nguyenchiemdu/flutter_barchart
version: 1.0.0+1
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true
